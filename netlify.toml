[build]
  base = "frontend"  # or whatever your frontend folder is
  command = "npm run build"
  publish = ".next"  # or "out" if using static export

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
